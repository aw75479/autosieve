"""autosieve -- multi-target Sieve script generation, IMAP alias extraction, and ManageSieve upload.

See :mod:`autosieve.cli` for the command-line entry point. Optional features
(vacation, notify, custom filters, OAuth2, backups) live under
:mod:`autosieve.features` and may be disabled by deleting the corresponding
module and its CLI registration.
"""

__version__ = "0.2.0"
